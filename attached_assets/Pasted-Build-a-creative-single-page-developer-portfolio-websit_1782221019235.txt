Build a creative, single-page developer portfolio website. This is NOT a generic template — it should feel like an award-worthy, modern portfolio with deliberate, smooth scroll-driven animation.

TECH STACK (important — keep it lightweight):
- Plain HTML, CSS, and vanilla JavaScript. Do NOT use React or any heavy framework.
- Use GSAP (GreenSock) with the ScrollTrigger plugin for all major animations. Load via CDN.
- Use modern CSS (custom properties, clamp() for fluid type, flexbox/grid). No CSS framework like Bootstrap.

DESIGN DIRECTION:
- Bold, oversized typography as the main visual element. Big confident headlines.
- Generous whitespace, strong visual hierarchy, a clean editorial feel.
- A refined color palette: dark background with one vivid accent color. [e.g. near-black #0a0a0a with an electric accent like #6c5ce7 — change to your taste]
- Use a distinctive Google Font pairing (one strong display font for headings, one clean sans-serif for body).
- Subtle grain/noise texture or a soft gradient is welcome, but keep it tasteful, not busy.

SECTIONS (in order):
1. Hero — my name "[YOUR NAME]" in large animated text, a short tagline ("[e.g. Front-end developer crafting interactive web experiences]"), and a scroll-down cue. Text should animate in on load (staggered reveal).
2. About — a short intro paragraph about me, with text that fades/slides in as it enters the viewport.
3. Projects — a grid or stacked list of 3–4 project cards. Each card has a title, short description, tech tags, and a placeholder image. Cards should animate in on scroll and have a smooth hover effect (slight scale + image zoom). Use placeholder content I can replace.
4. Skills — a clean list/row of my skills: [HTML, CSS, JavaScript, GSAP, etc.] that reveals on scroll.
5. Contact — my email "[YOUR EMAIL]" and links to [GitHub], [LinkedIn], etc., with a bold closing headline.

ANIMATION REQUIREMENTS:
- Use GSAP ScrollTrigger so elements reveal (fade + slide up) as the user scrolls to them.
- Add at least one "signature moment": pin the hero or a project section while content animates as the user scrolls past it.
- Add a custom animated cursor (a small circle that follows the mouse and subtly grows on hover over links/buttons).
- Smooth scrolling throughout.
- Keep all animation smooth at 60fps — animate transform and opacity, not layout properties.

REQUIREMENTS:
- Fully responsive (looks great on mobile and desktop). Animations should be tasteful/reduced on mobile.
- Respect prefers-reduced-motion for accessibility.
- Clean, well-commented code so I can easily edit content and colors later.
- Organize into index.html, style.css, and script.js.

Deliver a working, polished result I can preview immediately, then tell me exactly which lines to edit to swap in my own projects, images, and links.